#pragma once

namespace enums {
    /**
     * @brief Rappresenta una tipologia di angolo per WallSegment
     *
     */
    enum class WallAngleType {
        NONE = 0,
        START = 1,
        END = 2,
        BOTH = 3,
    };
};  // namespace enums