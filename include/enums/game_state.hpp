#pragma once

namespace enums {
    // TODO: descrizione di questo enumerator
    // (i suoi utilizzi)
    enum GameState {
        SCREEN_TO_SMALL,
        RUNNING,
        PAUSE,
        FINISH
    };
}